services:
  - type: web
    name: cacambix
    runtime: static
    buildCommand: yarn && yarn build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
