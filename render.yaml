services:
  - type: web
    name: adm-icell-fipe
    runtime: static
    buildCommand: yarn && yarn build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
